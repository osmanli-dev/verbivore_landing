# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# live CMS data — never tracked, so `git pull` on the server can't clobber
# content editors' changes. Fresh installs: run scripts/bootstrap-data.sh,
# which copies the committed snapshot from directus-cms/seed-data/.
/directus-cms/verbivore.db
/directus-cms/uploads/
*.db-shm
*.db-wal

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
